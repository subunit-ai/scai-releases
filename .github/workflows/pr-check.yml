# SCAI — PR-Check (tsc + vite + cargo test) für das PRIVATE Quell-Repo.
#
# WARUM hier (im PUBLIC scai-releases) statt im privaten subunit-scai:
# GitHub rechnet Actions-Minuten nach dem Repo ab, in dem der Workflow LÄUFT.
# `subunit-scai` ist privat → dessen Actions hängen an der Org-Billing-Sperre
# (Jobs sterben nach 3s mit 0 Steps). `scai-releases` ist public → KOSTENLOS.
# Der Quellcode wird per read-only Deploy-Key ausgecheckt (Muster build-all.yml).
#
# SICHERHEIT: Trigger ist AUSSCHLIESSLICH workflow_dispatch — Forks können den
# Workflow nicht mit unseren Secrets starten. Nur wer Schreibrechte auf DIESES
# Repo hat, kann ihn auslösen.
#
# AUSGELÖST VON: scripts/session/ship.sh im Quell-Repo (vor dem Squash-Merge).
#   gh workflow run pr-check.yml -R subunit-ai/scai-releases -f ref=session/<slug>
# ship.sh wartet auf die Conclusion dieses Runs und merged nur bei grün.

name: PR Check (Quell-Repo)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch/SHA im privaten Quell-Repo (z.B. session/halo-p1-agent)"
        required: true
        default: "main"
      trace_ref:
        description: "Optionaler exakter 40-Zeichen-SHA aus trace-tauri für den Standalone-Matrixcheck"
        required: false
        default: ""
      diagnostic_public_key_base64:
        description: "Optionaler einmaliger RSA-3072+-Public-Key im PEM-Base64-Format für verschlüsselte private Fehlerdiagnostik"
        required: false
        default: ""
      trace_bundle_public_key_base64:
        description: "Optionaler einmaliger RSA-3072+-Public-Key für einen verschlüsselten, intern signierten Trace-macOS-Bundle"
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: pr-check-${{ inputs.ref }}-${{ inputs.trace_ref }}
  cancel-in-progress: true

jobs:
  check:
    name: tsc + vite + cargo test
    runs-on: ubuntu-latest
    steps:
      - name: Öffentlichen Confidential-Runner auschecken
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2, immutable
        with:
          path: gate
          persist-credentials: false

      - name: Trace-Artefakt-Inputvertrag prüfen
        shell: bash
        env:
          TRACE_REF: ${{ inputs.trace_ref }}
          TRACE_BUNDLE_KEY: ${{ inputs.trace_bundle_public_key_base64 }}
        run: |
          set -euo pipefail
          if [ -n "$TRACE_BUNDLE_KEY" ] && [ -z "$TRACE_REF" ]; then
            echo "::error::Ein Trace-Bundle-Empfängerschlüssel erfordert einen exakten trace_ref."
            exit 1
          fi

      # Bun wird vor dem privaten Checkout bereitgestellt. Die gepinnte Action sieht
      # damit keinen proprietären Source-Tree; alle späteren Testausgaben bleiben im
      # Confidential-Runner.
      - name: Bun einrichten
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2, immutable

      # Klon über Git Bash statt actions/checkout+ssh-key (siehe build-all.yml).
      # Ref + Key über env, nicht inline → Injection-sicher.
      - name: Quellcode auschecken (privates Repo, read-only Deploy-Key)
        shell: bash
        env:
          SRC_REF: ${{ inputs.ref }}
          DEPLOY_KEY: ${{ secrets.SOURCE_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          bash "$GITHUB_WORKSPACE/gate/scripts/validate-private-source-ref.sh" "$SRC_REF"
          if [ -z "${DEPLOY_KEY:-}" ]; then
            echo "::error::SOURCE_DEPLOY_KEY fehlt."
            exit 1
          fi
          key_file="$HOME/.ssh/scai_src"
          cleanup() { rm -f "$key_file"; }
          trap cleanup EXIT
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$DEPLOY_KEY" > "$key_file"
          chmod 600 "$key_file"
          ssh-keyscan -t ed25519,rsa github.com >> "$HOME/.ssh/known_hosts" 2>/dev/null
          export GIT_SSH_COMMAND="ssh -i $key_file -o IdentitiesOnly=yes"
          git init src
          git -C src remote add origin git@github.com:subunit-ai/subunit-scai.git
          git -C src fetch --depth 1 -- origin "$SRC_REF"
          git -C src checkout --detach FETCH_HEAD
          ACTUAL_SHA=$(git -C src rev-parse HEAD)
          if printf '%s' "$SRC_REF" | grep -Eq '^[0-9a-f]{40}$' && [ "$ACTUAL_SHA" != "$SRC_REF" ]; then
            echo "::error::Checkout-Drift: erwartet $SRC_REF, erhalten $ACTUAL_SHA"
            exit 1
          fi
          cleanup
          trap - EXIT
          echo "ausgecheckt: $SRC_REF → $ACTUAL_SHA"

      - name: Node einrichten
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4, immutable
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: src/package-lock.json

      - name: Abhängigkeiten installieren
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" npm-ci npm ci

      - name: Frontend-Vertragstests ausführen
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" frontend-unit-tests bun test src/lib

      # Vendored subunit-CLI muss mit Manifest/Signing-Key/Bundle-Wiring synchron
      # bleiben — schlägt bei veraltetem/handgepatchtem Vendor fehl.
      - name: CLI-Drift-Wächter
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" cli-drift npm run drift-guard

      # Release-Meta: EINE Version in allen 5 Dateien + Changelog-Eintrag für sie.
      # Ohne den Eintrag bleibt das „Was ist neu"-Popup nach dem Update leer (genau
      # das passierte bei v0.16.0). Beim Lesen des Diffs sieht man das nicht — die
      # Maschine schon.
      - name: Release-Meta-Wächter
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" release-meta npm run check:release-meta

      # Jedes Plugin muss als eigenständiges Bundle bauen. Der App-Build (vite) deckt
      # das NICHT ab: die Plugin-Bundles laufen durch esbuild mit anderen Defines.
      # Gemessen: eine neue `import.meta.env.PROD`-Nutzung in lib/config.ts ließ den
      # chat-Bau scheitern — bemerkt erst beim Publish, und der ganze Release-Zug stand.
      - name: Plugin-Bundles bauen (Probe)
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" plugin-bundles npm run check:plugins

      # SCAI wurde als Demo gebaut und wird als Enterprise-Produkt verkauft. Seeds
      # ("Max Mustermann"), echte Kundennamen und Attrappen-Antworten dürfen nicht wieder
      # einsickern. Der Wächter hängt bewusst HIER und nicht nur im Quell-Repo: dessen
      # Actions sind billing-gesperrt (jeder Lauf stirbt nach ~4 s mit 0 Steps), der Step
      # dort läuft also nie. Diese Pipeline ist die einzige, die wirklich prüft.
      - name: Keine Demo-Daten im Produktcode
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" no-demo-data npm run check:no-demo-data

      - name: Bauen (tsc + vite)
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" frontend-build npm run build

      - name: Browser für Seh-Harnesse
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" playwright-install npx playwright install --with-deps chromium webkit

      # Der öffentliche Runner entwickelt sich unabhängig vom privaten Source-Repo.
      # Neue Feature-Proofs dürfen ältere/offene Source-Refs nicht rückwirkend brechen;
      # sobald das Harness im angeforderten Ref existiert, bleibt der Proof strikt.
      - name: Optionale Source-Proofs erkennen
        id: source_proofs
        working-directory: src
        shell: bash
        run: |
          if [ -f scripts/verify-support-diagnostics.mjs ]; then
            echo "support_diagnostics=true" >> "$GITHUB_OUTPUT"
          else
            echo "support_diagnostics=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-sentinel-forecast.mjs ]; then
            echo "sentinel_forecast=true" >> "$GITHUB_OUTPUT"
          else
            echo "sentinel_forecast=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-sentinel-crm-2026.mjs ]; then
            echo "sentinel_crm=true" >> "$GITHUB_OUTPUT"
          else
            echo "sentinel_crm=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-sentinel-routing.mjs ]; then
            echo "sentinel_routing=true" >> "$GITHUB_OUTPUT"
          else
            echo "sentinel_routing=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-chat-dock.mjs ]; then
            echo "chat_dock=true" >> "$GITHUB_OUTPUT"
          else
            echo "chat_dock=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-aenderungsjournal.mjs ]; then
            echo "aenderungsjournal=true" >> "$GITHUB_OUTPUT"
          else
            echo "aenderungsjournal=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-workgraph-blackbox.mjs ]; then
            echo "workgraph_blackbox=true" >> "$GITHUB_OUTPUT"
          else
            echo "workgraph_blackbox=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-moco-parity.mjs ]; then
            echo "moco_parity=true" >> "$GITHUB_OUTPUT"
          else
            echo "moco_parity=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f scripts/verify-workforce-capacity-overlays.mjs ]; then
            echo "workforce_capacity=true" >> "$GITHUB_OUTPUT"
          else
            echo "workforce_capacity=false" >> "$GITHUB_OUTPUT"
          fi
          subunit_call_host=false
          subunit_call_proof=false
          [ ! -f src/call/CallSessionHost.tsx ] || subunit_call_host=true
          [ ! -f scripts/verify-subunit-call.mjs ] || subunit_call_proof=true
          if [ "$subunit_call_host" = true ] && [ "$subunit_call_proof" = true ]; then
            echo "subunit_call=true" >> "$GITHUB_OUTPUT"
          elif [ "$subunit_call_host" = false ] && [ "$subunit_call_proof" = false ]; then
            echo "subunit_call=false" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Subunit-Call-Proof ist unvollständig; Call-Host und Proof-Harness müssen gemeinsam vorliegen."
            exit 1
          fi
          workspace_tabs=false
          workspace_app_plugins=false
          [ ! -f scripts/verify-workspace-tabs.mjs ] || workspace_tabs=true
          [ ! -f scripts/verify-workspace-app-plugins.mjs ] || workspace_app_plugins=true
          if [ "$workspace_tabs" = true ] && [ "$workspace_app_plugins" = true ]; then
            echo "workspace_browser=true" >> "$GITHUB_OUTPUT"
          elif [ "$workspace_tabs" = false ] && [ "$workspace_app_plugins" = false ]; then
            echo "workspace_browser=false" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Arbeitsbereich-Browser-Proof ist unvollständig; Web- und App-Modus-Harness müssen gemeinsam vorliegen."
            exit 1
          fi
          billing_revenue=false
          offers_revenue=false
          [ ! -f scripts/verify-billing-production.mjs ] || billing_revenue=true
          [ ! -f scripts/verify-offers-v01.mjs ] || offers_revenue=true
          if [ "$billing_revenue" = true ] && [ "$offers_revenue" = true ]; then
            echo "revenue_browser=true" >> "$GITHUB_OUTPUT"
          elif [ "$billing_revenue" = false ] && [ "$offers_revenue" = false ]; then
            echo "revenue_browser=false" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Revenue-Browser-Proof ist unvollständig; Billing- und Offers-Harness müssen gemeinsam vorliegen."
            exit 1
          fi

      - name: Subunit Call im Browser einschließlich Aufnahme und Native-Quit-Schutz beweisen
        id: subunit_call_proof
        if: always() && steps.source_proofs.outputs.subunit_call == 'true'
        working-directory: src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-subunit-call-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" subunit-call-proof node scripts/verify-subunit-call.mjs

      - name: Verschlüsselte Subunit-Call-Fehlerdiagnostik bereitstellen
        if: failure() && steps.subunit_call_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-subunit-call-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-subunit-call-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Revenue-Browser-Abhängigkeiten prüfen
        id: revenue_proof_dependencies
        if: steps.source_proofs.outputs.revenue_browser == 'true' || steps.source_proofs.outputs.workgraph_blackbox == 'true'
        working-directory: src
        shell: bash
        run: |
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" revenue-proof-dependencies npx --no-install playwright --version
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" revenue-proof-esbuild npx --no-install esbuild --version

      - name: Billing produktionsnah visuell und interaktiv beweisen
        id: billing_revenue_proof
        if: always() && steps.source_proofs.outputs.revenue_browser == 'true' && steps.revenue_proof_dependencies.outcome == 'success'
        working-directory: src
        shell: bash
        env:
          PROOF_RUN_ID: ci
          SCAI_BILLING_PROOF_DIR: ${{ runner.temp }}/scai-revenue-source-shots/billing
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-revenue-billing-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" billing-production-proof node scripts/verify-billing-production.mjs

      - name: Angebote v0.1 visuell und interaktiv beweisen
        id: offers_revenue_proof
        if: always() && steps.source_proofs.outputs.revenue_browser == 'true' && steps.revenue_proof_dependencies.outcome == 'success'
        working-directory: src
        shell: bash
        env:
          PROOF_RUN_ID: ci
          SCAI_OFFERS_PROOF_DIR: ${{ runner.temp }}/scai-revenue-source-shots/offers
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-revenue-offers-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" offers-v01-proof node scripts/verify-offers-v01.mjs

      - name: Workgraph Blackbox visuell und interaktiv beweisen
        id: workgraph_blackbox_proof
        if: always() && steps.source_proofs.outputs.workgraph_blackbox == 'true' && steps.revenue_proof_dependencies.outcome == 'success'
        working-directory: src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-workgraph-blackbox-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" workgraph-blackbox-proof node scripts/verify-workgraph-blackbox.mjs

      - name: MOCO-Paritaetsprotokoll visuell und interaktiv beweisen
        if: steps.source_proofs.outputs.moco_parity == 'true'
        working-directory: src
        shell: bash
        env:
          MOCO_PARITY_PROOF_OUT: ${{ runner.temp }}/scai-moco-parity-proof
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" moco-parity-proof node scripts/verify-moco-parity.mjs

      - name: Workforce-Kapazität und Abwesenheit visuell beweisen
        if: always() && steps.source_proofs.outputs.workforce_capacity == 'true'
        working-directory: src
        shell: bash
        env:
          SCAI_WORKFORCE_CAPACITY_PROOF_DIR: ${{ runner.temp }}/scai-workforce-capacity-proof
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" workforce-capacity-proof node scripts/verify-workforce-capacity-overlays.mjs

      - name: Revenue-Fixture-Screenshots geschlossen prüfen
        id: revenue_artifacts
        if: always() && ((steps.source_proofs.outputs.revenue_browser == 'true' && steps.billing_revenue_proof.outcome == 'success' && steps.offers_revenue_proof.outcome == 'success' && (steps.source_proofs.outputs.workgraph_blackbox != 'true' || steps.workgraph_blackbox_proof.outcome == 'success')) || (steps.source_proofs.outputs.revenue_browser != 'true' && steps.source_proofs.outputs.workgraph_blackbox == 'true' && steps.workgraph_blackbox_proof.outcome == 'success'))
        working-directory: src
        shell: bash
        env:
          BILLING_PROOF_DIR: ${{ runner.temp }}/scai-revenue-source-shots/billing
          OFFERS_PROOF_DIR: ${{ runner.temp }}/scai-revenue-source-shots/offers
          REVENUE_ARTIFACT_DIR: ${{ runner.temp }}/scai-revenue-browser-proof
        run: |
          if [ "${{ steps.source_proofs.outputs.revenue_browser }}" = true ]; then
            if [ "${{ steps.source_proofs.outputs.workgraph_blackbox }}" = true ]; then
              bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" revenue-proof-artifacts node "$GITHUB_WORKSPACE/gate/scripts/verify-revenue-proof-artifacts.mjs" "$BILLING_PROOF_DIR" "$OFFERS_PROOF_DIR" "$REVENUE_ARTIFACT_DIR" "$HOME/.cache/u1-shots/scai-workgraph-blackbox"
            else
              bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" revenue-proof-artifacts node "$GITHUB_WORKSPACE/gate/scripts/verify-revenue-proof-artifacts.mjs" "$BILLING_PROOF_DIR" "$OFFERS_PROOF_DIR" "$REVENUE_ARTIFACT_DIR"
            fi
          else
            bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" revenue-proof-artifacts node "$GITHUB_WORKSPACE/gate/scripts/verify-revenue-proof-artifacts.mjs" --workgraph-only "$HOME/.cache/u1-shots/scai-workgraph-blackbox" "$REVENUE_ARTIFACT_DIR"
          fi

      - name: Revenue-Browser-Proof-Screenshots sichern
        if: always() && steps.revenue_artifacts.outcome == 'success'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-revenue-browser-proof-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-revenue-browser-proof/
          if-no-files-found: error
          retention-days: 14

      - name: Verschlüsselte Billing-Proof-Fehlerdiagnostik bereitstellen
        if: failure() && steps.billing_revenue_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-revenue-billing-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-revenue-billing-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Verschlüsselte Offers-Proof-Fehlerdiagnostik bereitstellen
        if: failure() && steps.offers_revenue_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-revenue-offers-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-revenue-offers-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Verschlüsselte Workgraph-Proof-Fehlerdiagnostik bereitstellen
        if: failure() && steps.workgraph_blackbox_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-workgraph-blackbox-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-workgraph-blackbox-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Datenschutz-Diagnosepaket visuell und interaktiv beweisen
        id: support_diagnostics_proof
        if: steps.source_proofs.outputs.support_diagnostics == 'true'
        working-directory: src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-support-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" support-diagnostics-proof node scripts/verify-support-diagnostics.mjs

      - name: Diagnosepaket-Proof-Screenshots vorhanden
        if: always() && steps.support_diagnostics_proof.outcome != 'skipped'
        shell: bash
        run: find "$HOME/.cache/u1-shots/scai-support-diagnostics/" -type f -name '*.png' -print -quit | grep -q .

      # Proof und PNG-Praesenz bleiben harte Gates. Nur GitHubs externe
      # Artefakt-Ablage ist best effort, weil FinalizeArtifact sporadisch 403 liefert.
      - name: Diagnosepaket-Proof-Screenshots sichern
        if: always() && steps.support_diagnostics_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-support-diagnostics-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/scai-support-diagnostics/
          if-no-files-found: error
          retention-days: 14

      - name: Verschlüsselte Diagnosepaket-Fehlerdiagnostik bereitstellen
        if: failure() && steps.support_diagnostics_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-support-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-support-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Pages-Versionsverlauf und Restore visuell beweisen
        id: pages_proof
        working-directory: src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-pages-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" pages-recovery-proof node scripts/verify-pages-foundation.mjs

      - name: Pages-Proof-Screenshots vorhanden
        if: always() && steps.pages_proof.outcome != 'skipped'
        shell: bash
        run: find "$HOME/.cache/u1-shots/scai-pages/" -type f -name '*.png' -print -quit | grep -q .

      - name: Pages-Proof-Screenshots sichern
        if: always() && steps.pages_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-pages-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/scai-pages/
          if-no-files-found: error
          retention-days: 14

      - name: Verschlüsselte Pages-Fehlerdiagnostik bereitstellen
        if: failure() && steps.pages_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-pages-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-pages-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Meet-Plugin visuell und interaktiv beweisen
        working-directory: src
        env:
          PROOF_RUN_ID: ci
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" meet-visual-proof node scripts/verify-meet-plugin.mjs

      - name: Meet-Proof-Screenshots sichern
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-meet-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/scai-meet-plugin/ci/
          if-no-files-found: warn
          retention-days: 14

      # Aenderungsjournal (Shell-Insel "Meine Aenderungen": Zurueck/Vor ueber echte
      # Aenderungen, Liste mit Zeitstempeln) - gegen die Musterfirma, ohne Server.
      - name: Aenderungsjournal visuell und interaktiv beweisen
        id: aenderungsjournal_proof
        if: steps.source_proofs.outputs.aenderungsjournal == 'true'
        working-directory: src
        env:
          PROOF_RUN_ID: ci
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-aenderungsjournal-diagnostic.json', runner.temp) || '' }}
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" aenderungsjournal-proof node scripts/verify-aenderungsjournal.mjs

      - name: Aenderungsjournal-Proof-Screenshots sichern
        if: always() && steps.aenderungsjournal_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-aenderungsjournal-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/scai-aenderungsjournal/
          if-no-files-found: warn
          retention-days: 14

      - name: Verschlüsselte Änderungsjournal-Fehlerdiagnostik bereitstellen
        if: failure() && steps.aenderungsjournal_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-aenderungsjournal-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-aenderungsjournal-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Chat-Dock und lokale Laufzeitgrenze visuell beweisen
        id: chat_dock_proof
        if: steps.source_proofs.outputs.chat_dock == 'true'
        working-directory: src
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-chat-dock-diagnostic.json', runner.temp) || '' }}
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" chat-dock-visual-proof npm run proof:chat-dock

      - name: Chat-Dock-Proof-Screenshots vorhanden
        if: always() && steps.chat_dock_proof.outcome != 'skipped'
        shell: bash
        run: find "$HOME/.cache/u1-shots/scai-chat-dock/" -type f -name '*.png' -print -quit | grep -q .

      - name: Chat-Dock-Proof-Screenshots sichern
        if: always() && steps.chat_dock_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-chat-dock-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/scai-chat-dock/
          if-no-files-found: error
          retention-days: 14

      - name: Verschlüsselte Chat-Dock-Fehlerdiagnostik bereitstellen
        if: failure() && steps.chat_dock_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-chat-dock-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-chat-dock-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Sentinel CRM 2026 visuell und interaktiv beweisen
        id: sentinel_crm_proof
        if: steps.source_proofs.outputs.sentinel_crm == 'true'
        working-directory: src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-sentinel-crm-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" sentinel-crm-proof npm run proof:sentinel-crm

      - name: Sentinel-CRM-Proof-Screenshots vorhanden
        if: always() && steps.sentinel_crm_proof.outcome != 'skipped'
        shell: bash
        run: find "$HOME/.cache/u1-shots/sentinel-crm-2026/" -type f -name '*.png' -print -quit | grep -q .

      - name: Sentinel-CRM-Proof-Screenshots sichern
        if: always() && steps.sentinel_crm_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-sentinel-crm-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/sentinel-crm-2026/
          if-no-files-found: error
          retention-days: 14

      - name: Verschlüsselte Sentinel-CRM-Fehlerdiagnostik bereitstellen
        if: failure() && steps.sentinel_crm_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-sentinel-crm-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-sentinel-crm-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Sentinel Forecast Command Desk beweisen
        id: forecast_proof
        if: steps.source_proofs.outputs.sentinel_forecast == 'true'
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" sentinel-forecast-proof node scripts/verify-sentinel-forecast.mjs

      - name: Sentinel-Forecast-Screenshots vorhanden
        if: always() && steps.forecast_proof.outcome != 'skipped'
        shell: bash
        run: find "$HOME/.cache/u1-shots/sentinel-forecast/" -type f -name '*.png' -print -quit | grep -q .

      - name: Sentinel-Forecast-Screenshots sichern
        if: always() && steps.forecast_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-sentinel-forecast-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/sentinel-forecast/
          if-no-files-found: error
          retention-days: 14

      - name: Sentinel Lead Ownership und SLA Routing beweisen
        id: routing_proof
        if: steps.source_proofs.outputs.sentinel_routing == 'true'
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" sentinel-routing-proof node scripts/verify-sentinel-routing.mjs

      - name: Sentinel-Routing-Screenshots vorhanden
        if: always() && steps.routing_proof.outcome != 'skipped'
        shell: bash
        run: find "$HOME/.cache/u1-shots/sentinel-routing/" -type f -name '*.png' -print -quit | grep -q .

      - name: Sentinel-Routing-Screenshots sichern
        if: always() && steps.routing_proof.outcome != 'skipped'
        continue-on-error: true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-sentinel-routing-proof-${{ github.run_id }}
          path: ~/.cache/u1-shots/sentinel-routing/
          if-no-files-found: error
          retention-days: 14

      - name: Arbeitsbereich mit Tabs und geteilten Flächen im Web beweisen
        id: workspace_tabs_proof
        if: steps.source_proofs.outputs.workspace_browser == 'true'
        working-directory: src
        shell: bash
        env:
          SCAI_WORKSPACE_PROOF_OUT: ${{ runner.temp }}/scai-workspace-tabs
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-workspace-tabs-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" workspace-tabs-proof node scripts/verify-workspace-tabs.mjs

      - name: Arbeitsbereich mit App-Modus-Plugins beweisen
        id: workspace_app_plugins_proof
        if: steps.source_proofs.outputs.workspace_browser == 'true'
        working-directory: src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ inputs.diagnostic_public_key_base64 }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ inputs.diagnostic_public_key_base64 != '' && format('{0}/scai-workspace-app-plugins-diagnostic.json', runner.temp) || '' }}
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" workspace-app-plugins-proof node scripts/verify-workspace-app-plugins.mjs

      - name: Verschlüsselte Arbeitsbereich-Tabs-Fehlerdiagnostik bereitstellen
        if: failure() && steps.workspace_tabs_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-workspace-tabs-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-workspace-tabs-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Verschlüsselte Arbeitsbereich-App-Plugins-Fehlerdiagnostik bereitstellen
        if: failure() && steps.workspace_app_plugins_proof.outcome == 'failure' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: scai-workspace-app-plugins-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/scai-workspace-app-plugins-diagnostic.json
          if-no-files-found: error
          retention-days: 1

      - name: Arbeitsbereich-Proof-Screenshots geschlossen prüfen
        if: always() && steps.workspace_tabs_proof.outcome != 'skipped' && steps.workspace_app_plugins_proof.outcome != 'skipped'
        shell: bash
        env:
          WORKSPACE_TABS_PROOF_DIR: ${{ runner.temp }}/scai-workspace-tabs
        run: |
          test -s "$WORKSPACE_TABS_PROOF_DIR/desktop-dark-split.png"
          test -s "$WORKSPACE_TABS_PROOF_DIR/mobile-light-areas.png"
          test -s "$WORKSPACE_TABS_PROOF_DIR/mobile-dark.png"
          test -s "$HOME/.cache/u1-shots/scai-workspace-app-plugins/dashboard-two-instances.png"
          test -s "$HOME/.cache/u1-shots/scai-workspace-app-plugins/halo-two-instances.png"
          test -s "$HOME/.cache/u1-shots/scai-workspace-app-plugins/native-menu-bridge.png"

      # Rust-Kompilat + Unit-Tests. Tauri v2 linkt webkit2gtk/gtk auch für
      # `cargo test` → Systembibliotheken nötig.
      - name: Tauri-Systemabhängigkeiten
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev \
            libayatana-appindicator3-dev librsvg2-dev libxdo-dev libssl-dev \
            libasound2-dev

      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable, immutable

      # Der read-only Trace-Key wird erst nach allen Third-Party-Actions
      # bereitgestellt und nach dem privaten Cargo-Aufruf wieder entfernt.
      - name: Privaten Trace-Kern für Cargo freischalten (read-only)
        shell: bash
        env:
          TRACE_DEPLOY_KEY: ${{ secrets.TRACE_SOURCE_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${TRACE_DEPLOY_KEY:-}" ]; then
            echo "::error::TRACE_SOURCE_DEPLOY_KEY fehlt."
            exit 1
          fi
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$TRACE_DEPLOY_KEY" > "$HOME/.ssh/trace_src"
          chmod 600 "$HOME/.ssh/trace_src"
          cat >> "$HOME/.ssh/config" <<'EOF'
          Host github-trace
            HostName github.com
            User git
            IdentityFile ~/.ssh/trace_src
            IdentitiesOnly yes
          EOF
          chmod 600 "$HOME/.ssh/config"
          git config --global url."git@github-trace:subunit-ai/trace-tauri.git".insteadOf \
            "https://github.com/subunit-ai/trace-tauri.git"
          echo "CARGO_NET_GIT_FETCH_WITH_CLI=true" >> "$GITHUB_ENV"

      - name: cargo test (Sidecar-Trust-Modell + halo + verify-Suite)
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" cargo-test cargo test --manifest-path src-tauri/Cargo.toml --lib --features local-meet

      - name: Private Trace-Credentials entfernen
        if: always()
        shell: bash
        run: |
          rm -f "$HOME/.ssh/trace_src"
          git config --global --unset-all url.git@github-trace:subunit-ai/trace-tauri.git.insteadOf || true

  trace-standalone:
    name: Trace Standalone prüfen (${{ matrix.label }})
    if: inputs.trace_ref != ''
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            label: linux-core
            mode: core
          - runner: macos-15
            label: macos-arm64
            mode: native
          - runner: macos-15-intel
            label: macos-x64
            mode: native
          - runner: windows-2025
            label: windows-x64
            mode: native
            openssl_triplet: x64-windows-static-md
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Öffentlichen Confidential-Runner auschecken
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2, immutable
        with:
          path: gate
          persist-credentials: false

      - name: Exakten privaten Trace-Stand auschecken (read-only)
        shell: bash
        env:
          SRC_REF: ${{ inputs.trace_ref }}
          DEPLOY_KEY: ${{ secrets.TRACE_SOURCE_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          if ! printf '%s' "$SRC_REF" | grep -Eq '^[0-9a-f]{40}$'; then
            echo "::error::trace_ref muss ein unveränderlicher 40-Zeichen-SHA sein."
            exit 1
          fi
          if [ -z "${DEPLOY_KEY:-}" ]; then
            echo "::error::TRACE_SOURCE_DEPLOY_KEY fehlt."
            exit 1
          fi
          key_file="$HOME/.ssh/trace_matrix_src"
          cleanup() { rm -f "$key_file"; }
          trap cleanup EXIT
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$DEPLOY_KEY" > "$key_file"
          chmod 600 "$key_file"
          ssh-keyscan -t ed25519,rsa github.com >> "$HOME/.ssh/known_hosts" 2>/dev/null
          export GIT_SSH_COMMAND="ssh -i $key_file -o IdentitiesOnly=yes"
          git init trace-src
          git -C trace-src remote add origin git@github.com:subunit-ai/trace-tauri.git
          git -C trace-src fetch --depth 1 -- origin "$SRC_REF"
          git -C trace-src checkout --detach FETCH_HEAD
          ACTUAL_SHA=$(git -C trace-src rev-parse HEAD)
          if [ "$ACTUAL_SHA" != "$SRC_REF" ]; then
            echo "::error::Trace-Checkout-Drift: erwartet $SRC_REF, erhalten $ACTUAL_SHA"
            exit 1
          fi
          cleanup
          trap - EXIT
          echo "Trace ausgecheckt: $ACTUAL_SHA"

      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable, immutable
        with:
          components: rustfmt, clippy

      - name: Linux-Abhängigkeiten
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y libxcb1-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev

      - name: OpenSSL für Trace-SQLCipher (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          OPENSSL_TRIPLET: ${{ matrix.openssl_triplet }}
        run: |
          if (-not $env:OPENSSL_TRIPLET) { throw "OpenSSL-vcpkg-Triplet fehlt" }
          vcpkg install "openssl:$env:OPENSSL_TRIPLET"
          "OPENSSL_DIR=$env:VCPKG_INSTALLATION_ROOT\installed\$env:OPENSSL_TRIPLET" >> $env:GITHUB_ENV

      - name: Formatvertrag prüfen
        if: matrix.mode == 'core'
        working-directory: trace-src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-fmt cargo fmt -- --check

      - name: Plattformneutralen Core prüfen
        if: matrix.mode == 'core'
        working-directory: trace-src
        shell: bash
        run: |
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-core-check cargo check --locked
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-core-clippy cargo clippy --locked -- -D warnings
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-core-test cargo test --locked

      - name: Nativen Standalone-Host prüfen
        if: matrix.mode == 'native'
        working-directory: trace-src
        shell: bash
        env:
          SCAI_ENCRYPTED_DIAGNOSTIC_PUBLIC_KEY_BASE64: ${{ matrix.label == 'windows-x64' && inputs.diagnostic_public_key_base64 || '' }}
          SCAI_ENCRYPTED_DIAGNOSTIC_PATH: ${{ matrix.label == 'windows-x64' && inputs.diagnostic_public_key_base64 != '' && format('{0}/trace-windows-diagnostic.json', runner.temp) || '' }}
        run: |
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-native-check cargo check --locked --features host --bin trace-host
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-native-clippy cargo clippy --locked --features host --all-targets -- -D warnings
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-native-test cargo test --locked --features host
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-native-build cargo build --release --locked --features host --bin trace-host

      - name: Einmaligen Trace-Artefakt-Empfängerschlüssel prüfen
        if: matrix.label == 'macos-arm64' && inputs.trace_bundle_public_key_base64 != ''
        shell: bash
        env:
          RECIPIENT_PUBLIC_KEY_BASE64: ${{ inputs.trace_bundle_public_key_base64 }}
        run: |
          set -euo pipefail
          : > "$RUNNER_TEMP/trace-key-preflight"
          node "$GITHUB_WORKSPACE/gate/scripts/encrypt-confidential-log.mjs" \
            "$RUNNER_TEMP/trace-key-preflight" \
            "$RUNNER_TEMP/trace-key-preflight.envelope.json" \
            "$RECIPIENT_PUBLIC_KEY_BASE64"
          rm -f "$RUNNER_TEMP/trace-key-preflight" "$RUNNER_TEMP/trace-key-preflight.envelope.json"

      # Wichtig: Dieses private Source-Script läuft VOR dem Import der Signing-Secrets. Danach
      # werden ausschließlich fest im öffentlichen Gate definierte Apple-Werkzeuge ausgeführt.
      - name: Trace-App ohne vertrauliche Codeidentität vorpaketieren
        if: matrix.label == 'macos-arm64' && inputs.trace_bundle_public_key_base64 != ''
        working-directory: trace-src
        shell: bash
        run: |
          set -euo pipefail
          bundle_root="$RUNNER_TEMP/trace-host-macos-arm64-internal"
          version="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)"
          mkdir "$bundle_root"
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-macos-app-package \
            bash scripts/build-macos-app.sh \
              target/release/trace-host \
              "$bundle_root/Trace Host.app" \
              - \
              "$version"
          bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" trace-macos-setup-package \
            bash scripts/build-macos-setup-app.sh \
              "$bundle_root/Trace einrichten.app" \
              - \
              "$version"
          test -d "$bundle_root/Trace Host.app"
          test ! -L "$bundle_root/Trace Host.app"
          test -f "$bundle_root/Trace Host.app/Contents/Info.plist"
          test ! -L "$bundle_root/Trace Host.app/Contents/Info.plist"
          test -x "$bundle_root/Trace Host.app/Contents/MacOS/trace-host"
          test ! -L "$bundle_root/Trace Host.app/Contents/MacOS/trace-host"
          test -z "$(find "$bundle_root/Trace Host.app" -type l -print -quit)"
          test "$(plutil -extract CFBundleIdentifier raw "$bundle_root/Trace Host.app/Contents/Info.plist")" = "ai.subunit.trace-host"
          codesign --verify --deep --strict --verbose=2 "$bundle_root/Trace Host.app"
          test -d "$bundle_root/Trace einrichten.app"
          test ! -L "$bundle_root/Trace einrichten.app"
          test -f "$bundle_root/Trace einrichten.app/Contents/Info.plist"
          test ! -L "$bundle_root/Trace einrichten.app/Contents/Info.plist"
          test -x "$bundle_root/Trace einrichten.app/Contents/MacOS/trace-setup"
          test ! -L "$bundle_root/Trace einrichten.app/Contents/MacOS/trace-setup"
          test -z "$(find "$bundle_root/Trace einrichten.app" -type l -print -quit)"
          test "$(plutil -extract CFBundleIdentifier raw "$bundle_root/Trace einrichten.app/Contents/Info.plist")" = "ai.subunit.trace-setup"
          codesign --verify --deep --strict --verbose=2 "$bundle_root/Trace einrichten.app"
          ln -s "Trace Host.app/Contents/MacOS/trace-host" "$bundle_root/trace-host"
          cp README.md docs/PILOT-RUNBOOK.md "$bundle_root/"

      - name: Interne macOS-Codeidentität für Trace importieren
        if: matrix.label == 'macos-arm64' && inputs.trace_bundle_public_key_base64 != ''
        shell: bash
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          set -euo pipefail
          test -n "${APPLE_CERTIFICATE:-}"
          test -n "${APPLE_SIGNING_IDENTITY:-}"
          case "$APPLE_SIGNING_IDENTITY" in
            "Apple Development:"*|"Developer ID Application:"*) ;;
            *) echo "::error::Unerwartete Apple-Codeidentität für den internen Trace-Build."; exit 1 ;;
          esac
          certificate="$RUNNER_TEMP/trace-signing.p12"
          keychain="$RUNNER_TEMP/trace-signing.keychain-db"
          echo "$APPLE_CERTIFICATE" | base64 -d > "$certificate"
          security create-keychain -p trace-ci "$keychain"
          security set-keychain-settings -lut 21600 "$keychain"
          security unlock-keychain -p trace-ci "$keychain"
          security import "$certificate" -k "$keychain" \
            -P "${APPLE_CERTIFICATE_PASSWORD:-}" -T /usr/bin/codesign >/dev/null
          security list-keychains -d user -s "$keychain"
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k trace-ci "$keychain" >/dev/null
          security find-identity -v -p codesigning "$keychain" | grep -Fq "$APPLE_SIGNING_IDENTITY"
          echo "TRACE_INTERNAL_SIGNING_IDENTITY=$APPLE_SIGNING_IDENTITY" >> "$GITHUB_ENV"

      - name: Signierten Trace-App-Bundle bauen und einmalig verschlüsseln
        if: matrix.label == 'macos-arm64' && inputs.trace_bundle_public_key_base64 != ''
        working-directory: trace-src
        shell: bash
        env:
          RECIPIENT_PUBLIC_KEY_BASE64: ${{ inputs.trace_bundle_public_key_base64 }}
          SOURCE_SHA: ${{ inputs.trace_ref }}
        run: |
          set -euo pipefail
          bundle_root="$RUNNER_TEMP/trace-host-macos-arm64-internal"
          dmg="$RUNNER_TEMP/trace-host-macos-arm64-internal.dmg"
          delivery="$RUNNER_TEMP/trace-host-macos-arm64-internal.tar.gz"
          envelope="$RUNNER_TEMP/trace-host-macos-arm64-internal.tar.gz.envelope.json"
          version="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)"
          codesign --force --options runtime --timestamp \
            --sign "$TRACE_INTERNAL_SIGNING_IDENTITY" "$bundle_root/Trace Host.app"
          codesign --force --options runtime --timestamp \
            --sign "$TRACE_INTERNAL_SIGNING_IDENTITY" "$bundle_root/Trace einrichten.app"
          signature="$(codesign -dv --verbose=4 "$bundle_root/Trace Host.app" 2>&1)"
          test "$(sed -n 's/^Identifier=//p' <<<"$signature")" = "ai.subunit.trace-host"
          test "$(sed -n 's/^TeamIdentifier=//p' <<<"$signature")" = "Y23NYAZM6M"
          grep -Eq '^Authority=(Apple Development|Developer ID Application):' <<<"$signature"
          codesign --verify --deep --strict --verbose=2 "$bundle_root/Trace Host.app"
          setup_signature="$(codesign -dv --verbose=4 "$bundle_root/Trace einrichten.app" 2>&1)"
          test "$(sed -n 's/^Identifier=//p' <<<"$setup_signature")" = "ai.subunit.trace-setup"
          test "$(sed -n 's/^TeamIdentifier=//p' <<<"$setup_signature")" = "Y23NYAZM6M"
          grep -Eq '^Authority=(Apple Development|Developer ID Application):' <<<"$setup_signature"
          codesign --verify --deep --strict --verbose=2 "$bundle_root/Trace einrichten.app"
          jq -n \
            --arg source_sha "$SOURCE_SHA" \
            --arg version "$version" \
            '{schema_version:"1.0", source_sha:$source_sha, version:$version, bundle_identifier:"ai.subunit.trace-host", setup_bundle_identifier:"ai.subunit.trace-setup", team_identifier:"Y23NYAZM6M", distribution_policy:"internal-signed-not-notarized", customer_ready:false}' \
            > "$bundle_root/BUILD-EVIDENCE.json"
          hdiutil create -volname "Subunit Trace Host Internal" \
            -srcfolder "$bundle_root" -ov -format UDZO "$dmg" >/dev/null
          codesign --force --sign "$TRACE_INTERNAL_SIGNING_IDENTITY" "$dmg"
          codesign --verify --strict --verbose=2 "$dmg"
          tar -czf "$delivery" -C "$RUNNER_TEMP" \
            "$(basename "$bundle_root")" "$(basename "$dmg")"
          node "$GITHUB_WORKSPACE/gate/scripts/encrypt-confidential-log.mjs" \
            "$delivery" "$envelope" "$RECIPIENT_PUBLIC_KEY_BASE64"
          rm -rf "$bundle_root" "$dmg" "$delivery"

      - name: Verschlüsselten internen Trace-Mac-Bundle bereitstellen
        if: matrix.label == 'macos-arm64' && inputs.trace_bundle_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: trace-macos-arm64-internal-encrypted-${{ github.run_id }}
          path: ${{ runner.temp }}/trace-host-macos-arm64-internal.tar.gz.envelope.json
          if-no-files-found: error
          retention-days: 1

      - name: Ephemere Trace-Signingdaten entfernen
        if: always() && matrix.label == 'macos-arm64' && inputs.trace_bundle_public_key_base64 != ''
        shell: bash
        run: |
          security delete-keychain "$RUNNER_TEMP/trace-signing.keychain-db" >/dev/null 2>&1 || true
          rm -f "$RUNNER_TEMP/trace-signing.p12" \
            "$RUNNER_TEMP/trace-host-macos-arm64-internal.dmg" \
            "$RUNNER_TEMP/trace-host-macos-arm64-internal.tar.gz"
          rm -rf "$RUNNER_TEMP/trace-host-macos-arm64-internal"

      - name: Verschlüsselte Trace-Windows-Fehlerdiagnostik bereitstellen
        if: failure() && matrix.label == 'windows-x64' && inputs.diagnostic_public_key_base64 != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4, immutable
        with:
          name: trace-windows-encrypted-diagnostic-${{ github.run_id }}
          path: ${{ runner.temp }}/trace-windows-diagnostic.json
          if-no-files-found: error
          retention-days: 1

  native-trace:
    name: SCAI + Trace nativ kompilieren (${{ matrix.label }})
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            label: macos-arm64
          - runner: windows-2025
            label: windows-x64
            openssl_triplet: x64-windows-static-md
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Öffentlichen Confidential-Runner auschecken
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2, immutable
        with:
          path: gate
          persist-credentials: false

      - name: Quellcode auschecken (privates Repo, read-only Deploy-Key)
        shell: bash
        env:
          SRC_REF: ${{ inputs.ref }}
          DEPLOY_KEY: ${{ secrets.SOURCE_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          bash "$GITHUB_WORKSPACE/gate/scripts/validate-private-source-ref.sh" "$SRC_REF"
          if [ -z "${DEPLOY_KEY:-}" ]; then
            echo "::error::SOURCE_DEPLOY_KEY fehlt."
            exit 1
          fi
          key_file="$HOME/.ssh/scai_src"
          cleanup() { rm -f "$key_file"; }
          trap cleanup EXIT
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$DEPLOY_KEY" > "$key_file"
          chmod 600 "$key_file"
          ssh-keyscan -t ed25519,rsa github.com >> "$HOME/.ssh/known_hosts" 2>/dev/null
          export GIT_SSH_COMMAND="ssh -i $key_file -o IdentitiesOnly=yes"
          git init src
          git -C src remote add origin git@github.com:subunit-ai/subunit-scai.git
          git -C src fetch --depth 1 -- origin "$SRC_REF"
          git -C src checkout --detach FETCH_HEAD
          ACTUAL_SHA=$(git -C src rev-parse HEAD)
          if printf '%s' "$SRC_REF" | grep -Eq '^[0-9a-f]{40}$' && [ "$ACTUAL_SHA" != "$SRC_REF" ]; then
            echo "::error::Checkout-Drift: erwartet $SRC_REF, erhalten $ACTUAL_SHA"
            exit 1
          fi
          cleanup
          trap - EXIT
          echo "ausgecheckt: $SRC_REF → $ACTUAL_SHA"

      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable, immutable

      - name: OpenSSL für Trace-SQLCipher (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          OPENSSL_TRIPLET: ${{ matrix.openssl_triplet }}
        run: |
          if (-not $env:OPENSSL_TRIPLET) { throw "OpenSSL-vcpkg-Triplet fehlt" }
          vcpkg install "openssl:$env:OPENSSL_TRIPLET"
          "OPENSSL_DIR=$env:VCPKG_INSTALLATION_ROOT\installed\$env:OPENSSL_TRIPLET" >> $env:GITHUB_ENV

      - name: Privaten Trace-Kern für Cargo freischalten (read-only)
        shell: bash
        env:
          TRACE_DEPLOY_KEY: ${{ secrets.TRACE_SOURCE_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${TRACE_DEPLOY_KEY:-}" ]; then
            echo "::error::TRACE_SOURCE_DEPLOY_KEY fehlt."
            exit 1
          fi
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$TRACE_DEPLOY_KEY" > "$HOME/.ssh/trace_src"
          chmod 600 "$HOME/.ssh/trace_src"
          cat >> "$HOME/.ssh/config" <<'EOF'
          Host github-trace
            HostName github.com
            User git
            IdentityFile ~/.ssh/trace_src
            IdentitiesOnly yes
          EOF
          chmod 600 "$HOME/.ssh/config"
          git config --global url."git@github-trace:subunit-ai/trace-tauri.git".insteadOf \
            "https://github.com/subunit-ai/trace-tauri.git"
          echo "CARGO_NET_GIT_FETCH_WITH_CLI=true" >> "$GITHUB_ENV"

      # Kein Paket, Tag oder Release: dieses Gate kompiliert exakt die nativen
      # Versandfeatures, damit der private Trace-Pin nicht erst im Release auffällt.
      - name: SCAI mit nativer Trace-/Meet-Engine prüfen
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" native-cargo-check cargo check --locked --manifest-path src-tauri/Cargo.toml --features local-meet

      # Ein zusätzlicher src/bin-Helfer darf nie wieder als Tauri-Produkt im
      # Installer landen. Cargo-Metadata muss exakt subunit-scai aus main.rs zeigen.
      - name: Eindeutiges Produkt-Binary beweisen
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" native-product-binary node "$GITHUB_WORKSPACE/gate/scripts/verify-product-binary.mjs" src-tauri/Cargo.toml

      # A1 / Native Auth: Ein Linux-Erfolg reicht nicht als Beweis für den
      # ausgelieferten Desktop-Pfad. Derselbe gepinnte SCAI-Stand muss die
      # PKCE-/State-/Redirect-Bindung auf unseren echten Release-Runners
      # (macOS ARM64 und Windows x64) ausführen, nicht nur kompilieren.
      - name: Native PKCE-Sitzungstests ausführen
        working-directory: src
        shell: bash
        run: bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" native-pkce-tests cargo test --locked --manifest-path src-tauri/Cargo.toml --features local-meet auth::tests -- --nocapture

      # Anders als die Unit-Tests nutzt dieses explizite Smoke-Example den
      # echten macOS-Schlüsselbund beziehungsweise Windows Credential Manager.
      # Es bleibt außerhalb der Tauri-Binärziele; der zufällige Canary wird nie
      # ausgegeben und im selben Lauf gelöscht.
      - name: Echten OS-Credential-Store beweisen
        working-directory: src
        env:
          SCAI_A1_KEYRING_SMOKE: "1"
        shell: bash
        run: |
          set -euo pipefail
          if grep -Eq '^[[:space:]]*a1-keyring-smoke[[:space:]]*=' src-tauri/Cargo.toml; then
            bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" native-keyring-smoke \
              cargo run --locked --manifest-path src-tauri/Cargo.toml \
              --features a1-keyring-smoke --example a1_keyring_smoke
          else
            bash "$GITHUB_WORKSPACE/gate/scripts/run-confidential.sh" native-keyring-smoke \
              cargo run --locked --manifest-path src-tauri/Cargo.toml \
              --example a1_keyring_smoke
          fi

      - name: Private Trace-Credentials entfernen
        if: always()
        shell: bash
        run: |
          rm -f "$HOME/.ssh/trace_src"
          git config --global --unset-all url.git@github-trace:subunit-ai/trace-tauri.git.insteadOf || true
